/**
 * Dummy repository for initial admin-panel screen.
 * Replace with real API/data access when needed.
 */
export async function getDummyMessage(): Promise<string> {
  return Promise.resolve("Welcome to Admin Panel");
}
